1) buat file .env di folder root
2) lalu isi dengan
"
PORT= {Port yang kamu gunakan misalnya}
URL_MONGODB_DEV=mongodb://{alamat IP server MongoDB}:{nomor port MongoDB}/{nama database}
"
3) Lalu ketik "npm run dev" di terminal untuk menjalankannya 